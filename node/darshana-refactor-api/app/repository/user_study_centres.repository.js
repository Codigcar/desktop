const BaseRepository = require('./base.repository')

class UserStudyCentresRepository extends BaseRepository {
  constructor(model) {
    super(model)
  }
}

module.exports = UserStudyCentresRepository
